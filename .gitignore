target/
coverage/
# Player/runtime saves are not committed; committed content lives in
# data/areas (world blueprints) and data/socials (social overrides).
/data/*
!/data/areas/
!/data/socials/
# Per-crate test scratch data dirs.
crates/*/data/

# Agent worktrees and local planning (not committed)
.claude/
.planning/
